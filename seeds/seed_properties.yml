version: 2

seeds:
  - name: "country_codes"
    config:
      quote_columns: true 
      column_types: {s_no: varchar}
      delimiter: "|"
      alias: "dim_country"
      full_refresh: true
